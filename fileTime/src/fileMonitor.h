#ifndef __FILE_MONITOR_H
#define __FILE_MONITOR_H

#include <iostream>
#include <ctime>

class FileMonitor {
    public:
        FileMonitor(std::string filePath);
        ~FileMonitor();
};

#endif // __FILE_MONITOR_H
